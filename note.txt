update_context(cmd_output, result_dict, 'swd_DPCTimeout_Context')

match = re.search(r'\d+\.\d+', OS_Version_ID)

if match:
    OS_Version_ID = match.group()
    logger.info(f"提取的数字部分：{OS_Version_ID}")  # 输出：9600.17238
else:
    logger.info("未找到数字部分")

get_list_text_count
cmd_output_list = cmd_output.splitlines()
PnP_6_run(result_dict, current_step)

analyze_v_run(result_dict, current_step)

result_dict = thread_run(result_dict, current_step)

result_dict = process_vm_run(result_dict, current_step)

result_dict = locks_run(result_dict, current_step)

BSOD_Supcious_Driver = result_dict.get('BSOD_Supcious_Driver', None)
if BSOD_Supcious_Driver == 'NDIS Driver':
    result_dict = ndis_run(result_dict, current_step)

BSOD_Supcious_Device = result_dict.get('BSOD_Supcious_Device', None)
if BSOD_Supcious_Device == 'USB':
    result_dict = usb_run(result_dict, current_step)

 result_dict = storage_run(result_dict, current_step)